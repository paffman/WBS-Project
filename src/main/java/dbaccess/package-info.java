/**
 * Represents the interface to the database. It contains all the interfaces and
 * their implementations for the individual database tables. It also contains
 * some helper classes to make the interaction with the database easier.
 */
package dbaccess;